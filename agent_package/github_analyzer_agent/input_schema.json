{
  "title": "AgentInputs",
  "type": "object",
  "properties": {
    "repo_url": {
      "title": "Repository URL",
      "type": "string",
      "description": "Full URL of the GitHub repository (e.g., https://github.com/owner/repo)"
    },
    "max_files_to_analyze": {
      "default": 10,
      "title": "Max Files to Analyze",
      "type": "integer"
    },
    "focus_areas": {
      "items": {
        "type": "string"
      },
      "title": "Focus Areas",
      "type": "array",
      "description": "Specific folders or file types to focus on (e.g., 'src', '.py')"
    }
  },
  "required": ["repo_url"]
}
