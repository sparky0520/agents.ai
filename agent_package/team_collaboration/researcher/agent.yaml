name: Researcher Agent
version: 0.1.0
runtime: python:3.12
framework: langgraph
entrypoint: researcher_agent.py

description: |
  Conducts preliminary research on a given topic and provides structured notes.

inputs:
  schema_file: input_schema.json

outputs:
  schema_file: output_schema.json

environment:
  variables: []
