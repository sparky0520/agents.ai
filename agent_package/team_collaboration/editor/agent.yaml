name: Editor Agent
version: 0.1.0
runtime: python:3.12
framework: langgraph
entrypoint: editor_agent.py

description: |
  Reviews and polishes a draft article, providing a final version and critique.

inputs:
  schema_file: input_schema.json

outputs:
  schema_file: output_schema.json

environment:
  variables: []
